/* eslint-disable */
import { actionType } from '@/store/actionType'

const initalState = {
  subject: null,
}

export default function reducer(state = initalState, action){
  switch (action.type) {
    default: {
      return state
    }
  }
}
