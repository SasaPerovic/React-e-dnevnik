/* eslint-disable */
import styled from 'styled-components'
export const Logo = styled.span`
  position: relative; 
  top: ${props => (props.header ? '5px' : '0')};
  display: block;
  align-self: flex-start;
 

  img {
    position: relative;
    display: block;
     width: 100px;
     margin-left: 70px;

}
  }
`
